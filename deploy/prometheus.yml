global:
  scrape_interval: 1s # Интервал вытягивания данных

scrape_configs:
  - job_name: 'dto-controller'
    static_configs:
      # host.docker.internal — это адрес хоста внутри контейнера
      - targets: ['host.docker.internal:9100']